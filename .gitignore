.DS_Store
.claude/
# .planning/
# .superpowers/
.playwright/
.playwright-cli/
.worktrees/
node_modules/
*.log

# Auto-generated by `supabase start` — contains local dev URLs + demo keys
SUPABASE.md

# Phase 14 eval runner cache (D-A4 — PDFs lazy-fetched per run, never committed)
.tmp/eval-pdfs/

# Phase 14 eval runner outputs — timestamped runs accumulate per `npm run eval`; not source-of-truth
.planning/eval/runs/

# Local editor / tool workspaces — not relevant to repo
.claudian/
.obsidian/
